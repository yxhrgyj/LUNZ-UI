import { LaddaDirective } from './ladda.directive';

describe('LaddaDirective', () => {
  it('should create an instance', () => {
    const directive = new LaddaDirective();
    expect(directive).toBeTruthy();
  });
});
