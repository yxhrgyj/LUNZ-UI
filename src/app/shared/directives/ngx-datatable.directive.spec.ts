import { NgxDatatableDefaultSettingsDirective } from './ngx-datatable-default-settings.directive';

describe('NgxDatatableDefaultSettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxDataTableDirective();
    expect(directive).toBeTruthy();
  });
});
