import { BsDateValueDirective } from './bs-date-value.directive';

describe('BsDateValueDirective', () => {
  it('should create an instance', () => {
    const directive = new BsDateValueDirective();
    expect(directive).toBeTruthy();
  });
});
